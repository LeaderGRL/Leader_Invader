# Leader Invader

Private development repository for the deterministic visual-CPU GitHub README experiment.

The first implementation is being developed on a feature branch and will land through a pull request once CI and the generated SVG are validated.
