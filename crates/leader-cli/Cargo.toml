[package]
name = "leader-cli"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
leader-core = { path = "../leader-core" }
leader-svg = { path = "../leader-svg" }

[lints]
workspace = true
