// Production front-page rendering is intentionally isolated in a dedicated
// source file so the GitHub artifact can evolve without coupling presentation
// concerns back into the deterministic machine core.
include!("frontpage_v2.rs");
